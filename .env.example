# サーバー側だけで使う秘密値。ブラウザへは渡らない。
# 値は各自が直接設定する（このファイルに実値を書かない）。
OPENAI_API_KEY=
MATTA_DEMO_PIN=

# 任意設定（未設定時は既定値）
# MATTA_CHAT_MODEL=gpt-5.6-luna
# MATTA_EMBEDDING_MODEL=text-embedding-3-small
# MATTA_MIN_SIMILARITY=0.3
